import React from 'react';
import Home from './components/Home';

const Portfolio: React.FC = () => {

  //Active on navbar, scroll on navbar on click, grid correction/mobile responsive

  return (
    <section>
        <Home/>
    </section>
  )
}

export default Portfolio;
